<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.tedu.mall.sso.mapper.admin.AdminLoginLogMapper">

    <insert id="insertAdminLoginLog">
        insert into ams_login_log (
        admin_id,
        username,
        nickname,
        ip,
        user_agent,
        gmt_create,
        gmt_login,
        gmt_modified
        ) values (
        #{adminId},
        #{username},
        #{nickname},
        #{ip},
        #{userAgent},
        #{gmtCreate},
        #{gmtLogin},
        #{gmtModified}
        )
    </insert>

</mapper>
